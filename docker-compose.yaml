services:
  database:
    container_name: redis_container
    image: redis
    build: ./redis
    hostname: localhost
    ports:
      - "6379:6379"
  application:
    container_name: desafio_application
    image: desafio-ia
    links:
      - database
    ports:
      - "8080:8080"